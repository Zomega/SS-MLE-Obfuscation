# Define some sort of language that only compiles to programs in NC (i.e. circuts)?

# Example sample
# input bit x[0:3]
# y[3] = 
# output bit y[3]

# Build python bindings to these programs through compilation